<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    {{!-- bootstrap --}}
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" 
    integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" 
    crossorigin="anonymous">
    <title>Teachers</title>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
        <a class="navbar-brand text-primary-emphasis" href="/">Schools</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="/" class="nav-link">Homepage</a>
                </li>
                <li class="nav-item">
                    <a href="/studentsReg" class="nav-link">Add Students</a>
                </li>
                <li class="nav-item">
                    <a href="/teacherReg" class="nav-link">Add Teacher</a>
                </li>
                <li class="nav-item">
                    <a href="/hod" class="nav-link">Head Of Department</a>
                </li>
            </ul>
        </div>
    </nav>
    <br>
    <br>
    <br><br><br><br>
    <form action="/teacher" method="POST">
        <div class="dropdown text-center">
            <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
             Select Teacher
            </button>
            <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                {{#each listOfTeachers}}
                    <button class="dropdown-item" type="submit" name="teacherId" value="{{id}}">{{first_name}}</button>
                {{/each}}
            </div>
        </div>  
    </form>
    <p></p>
    <br><br>
    <h4>Bellow is the list of <strong>Subject/s</strong> that {{teachersName}} teaches.</h4>
    <br>
    <table class="table table-hover table-dark">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Subject Name</th>
            </tr>
        </thead>
        <tbody>
            {{#each listOfSubject}}
            <tr>
                <th scope="row">{{id}}</th>
                <td>{{name}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table><br>
    <div class="row">
        <div class="col-md-6 mb-3">
            <form action="/clearingSubject" method="post">
                <input type="hidden" name="teachersName" value="{{teachersName}}">
                <div class="form-group">
                    <select name="subjectId" class="form-control">
                    {{#each listOfSubject}}
                        <option value="{{id}}">{{name}}</option>
                    {{/each}}
                    </select>
                </div>
                <button type="submit" class="btn btn-danger">Clear Subject</button>
            </form>
        </div>
        <div class="col-md-6 mb-3">
            <form action="/addingSubjectToTeacher" method="post">
                <input type="hidden" name="teachersName" value="{{teachersName}}">
                <div class="form-group">
                    <select name="subjectId" class="form-control">
                    {{#each availableSubject}}
                        <option value="{{id}}">{{name}}</option>
                    {{/each}}
                    </select>
                </div>
                <button type="submit" class="btn btn-success">Add Teacher</button>
            </form>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/popper.js@1.12.9/dist/umd/popper.min.js" integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
</body>
</html>